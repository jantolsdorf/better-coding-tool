{
  "name": "better-coding-tool",
  "version": "0.1.0",
  "description": "A small browser-based tool for qualitative coding of text documents.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.3.1",
    "vite-plugin-singlefile": "^2.3.3"
  },
  "dependencies": {
    "fflate": "^0.8.3"
  }
}
